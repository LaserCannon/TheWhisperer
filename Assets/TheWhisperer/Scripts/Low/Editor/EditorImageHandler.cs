using UnityEngine;
using System.Collections;

public class EditorImageHandler : MonoBehaviour
{

	//TODO: Create a static dictionary that stores images at editor runtime, so we can reduce the amount of times we load textures during GUI calls
	
}
